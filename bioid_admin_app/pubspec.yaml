name: bioid_admin_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  dio: ^5.9.2
  supabase_flutter: ^2.14.0 # Kept the higher version and removed the duplicate
  camera: ^0.12.0+1
  image: ^4.9.1
  path_provider: ^2.1.5
  google_mlkit_face_detection: ^0.13.2
  flutter_animate: ^4.5.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true